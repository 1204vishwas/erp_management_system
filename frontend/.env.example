# Base URL for the backend API.
# In dev the Vite proxy forwards /api to http://localhost:3000, so this can stay "/api".
# In production set it to your deployed backend, e.g. https://your-api.onrender.com/api
VITE_API_URL=/api
